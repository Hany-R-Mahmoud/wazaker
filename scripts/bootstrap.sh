#!/usr/bin/env bash

set -euo pipefail

echo "wazaker project bootstrap"
echo "This script is intentionally minimal for phase 0."
echo "Next iterations can extend it to validate local tooling and env setup."
