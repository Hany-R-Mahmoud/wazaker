[
  {
    "id": "SpeechQa01",
    "name": "Speech QA Regression",
    "active": false,
    "isArchived": false,
    "nodes": [
      {
        "parameters": {
          "rule": {
            "interval": [
              {
                "field": "days",
                "daysInterval": 1,
                "triggerAtHour": 12,
                "triggerAtMinute": 15
              }
            ]
          }
        },
        "type": "n8n-nodes-base.scheduleTrigger",
        "typeVersion": 1.3,
        "position": [
          0,
          0
        ],
        "name": "Schedule Trigger"
      },
      {
        "parameters": {
          "jsCode": "const runnerBase = $env.AUTOMATION_RUNNER_BASE_URL;\nconst runnerToken = $env.AUTOMATION_RUNNER_TOKEN;\nconst headers = {\n  'Content-Type': 'application/json',\n  ...(runnerToken ? { Authorization: `Bearer ${runnerToken}` } : {}),\n};\nconst response = await fetch(`${runnerBase}/jobs`, {\n  method: 'POST',\n  headers,\n  body: JSON.stringify({ action: 'speech_qa_report', mode: 'sync', args: [] }),\n});\nconst raw = await response.text();\nif (!response.ok) throw new Error(`speech_qa_report failed: ${response.status} ${raw}`);\nconst result = raw ? JSON.parse(raw) : {};\nconst output = result.stdout ? JSON.parse(result.stdout) : {};\nreturn [{ json: { result, output } }];"
        },
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          260,
          0
        ],
        "name": "Run Speech QA Report"
      }
    ],
    "connections": {
      "Schedule Trigger": {
        "main": [
          [
            {
              "node": "Run Speech QA Report",
              "type": "main",
              "index": 0
            }
          ]
        ]
      }
    },
    "settings": {
      "executionOrder": "v1",
      "binaryMode": "separate"
    },
    "versionId": "25b25488-ab74-42d8-a6ef-b44bd2f491fc",
    "pinData": {},
    "tags": []
  }
]
